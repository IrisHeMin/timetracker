# Time Tracker：从一个工程师痛点到一个可分享小工具的产品化思考

> 一个用半天时间做出来的小 web app，背后是怎么想清楚每一步的？
>
> 这篇文档不是技术教程，而是一份**产品经理视角的复盘**：怎么定义问题、怎么取舍、怎么选工具、怎么迭代。给所有"我也想做点小东西"的工程师朋友们做参考。

---

## 一、问题定义：从一个真实痛点开始

### 用户故事

> 作为一名 Microsoft 技术支持工程师，**我每天同时处理 5+ 个 case**。
>
> 一天里我可能：早上 9 点看 case A 的 NDIS 日志，9:45 客户突然来电问 case B，10:20 又回到 case A 继续 research，11 点同事拉我开会讨论 case C……
>
> 到了下班记 labor 的时候，我要回答："今天在 case A 上花了多少分钟？"
>
> **我答不上来。** 完全凭感觉。结果就是要么少记吃亏，要么多记心虚。

### 问题的本质

很多人会以为这是"我记性不好"的问题，但拆开看其实是三个**不同性质**的问题叠加：

| 层级 | 表面问题 | 真正问题 |
|---|---|---|
| 1. 数据采集 | "我记不住每段时间花在哪" | **没有低成本的实时记录手段** |
| 2. 数据归类 | "我不知道某个 case 总共花了多久" | **多线程任务难以归并** |
| 3. 数据使用 | "我记的 labor 不准" | **没有可信的数据支撑决策** |

很多类似的工具（Toggl、Clockify 等）只解决了第 1 层，第 2 层（按业务对象自动归类）通常需要手动打 tag、配项目，**成本高到不愿意坚持**。

### 关键洞察 💡

**真正阻止用户使用时间追踪工具的，不是工具难用，而是"记录-归类"的总摩擦成本超过了用户能容忍的阈值。**

只要总摩擦超过 **5 秒/次**，工程师就不会用——因为他们的工作切换本身就比这快。

这个洞察决定了我后续所有的产品决策。

---

## 二、灵感来源：柳比歇夫的事件统计法

俄国昆虫学家柳比歇夫用 56 年坚持一件事：**精确记录每段时间花在了什么上面**。他靠这套方法做出了百倍于常人的产出。

但他的方法在数字时代有两个挑战：
1. 手写记录跟不上现代多线程切换的速度
2. 手动分类汇总耗时巨大

**产品定位由此确定**：
> "把柳比歇夫的事件统计法压缩成 2 秒一次按键的极简操作。让工程师能用最低摩擦记录、最高精度复盘。"

这一句话既是 slogan，也是后续所有功能取舍的**唯一标尺**。

---

## 三、MVP 设计：用最少功能验证假设

在动手前，我先列了一个"功能候选池"：

| 想做的功能 | 当下是否做？ | 决策依据 |
|---|---|---|
| ✅ 输入任务名 + Start/Stop 计时 | 做 | MVP 核心，不做就没产品 |
| ✅ 今日汇总（按任务名分组） | 做 | 验证"看到数据"的价值 |
| ✅ 数据持久化（localStorage） | 做 | 关电脑数据不能丢 |
| ⚠️ 趋势图（周/月） | 做 | 柳比歇夫的"复盘"环节，必要 |
| ⚠️ 番茄钟 | 做 | 早期未做，用户反馈后加入（见后续迭代）|
| ❌ Case ID 自动归类 | 不做 | 早期未想到，用户提了才发现是杀手锏 |
| ❌ 团队协作 / 云同步 | 不做 | 偏离"个人工具"定位 |
| ❌ Outlook / D365 集成 | 不做 | 复杂度爆炸，违背"轻量"原则 |
| ❌ 移动端原生 app | 不做 | Web 就够用，不值得多平台开发 |

**MVP 原则**：
- **少做不等于简陋**——核心动作必须极致流畅
- **能用键盘绝不用鼠标**：Tab/Enter 全键盘流
- **0 学习成本**：打开就知道怎么用，不需要任何说明

---

## 四、关键决策：技术栈与形态选择

### 决策 1：Web App vs Native App？

| 方案 | 优点 | 缺点 |
|---|---|---|
| **纯 Web (HTML+JS)** | 0 安装、跨平台、易分享、几 KB | 浏览器依赖、数据存浏览器 |
| Electron 桌面 | 原生体验、可访问系统 API | 几百 MB、构建复杂 |
| 原生 Windows | 性能最佳 | 单平台、开发慢、分发难 |

**选了 Web**。理由：
- 用户场景就是在电脑前工作 → 浏览器一定开着
- 12 KB vs 200 MB，分享门槛差 4 个数量级
- 用 Edge `--app=` 模式可以**伪装成原生 app**（独立窗口、独立任务栏图标），用户感知不到是网页

### 决策 2：有后端还是纯前端？

| 方案 | 优点 | 缺点 |
|---|---|---|
| **纯前端 + localStorage** | 0 运维成本、数据隐私、可离线 | 数据不能跨设备 |
| 前后端分离 + 云 DB | 数据可同步 | 需要服务器、隐私风险、要登录 |

**选了纯前端**。理由：
- 工程师对"case 号上云"非常敏感（隐私 / 公司合规）
- 个人工具不需要协作 → 不需要服务器
- 零运维 = 项目寿命可以无限长
- 数据迁移用 JSON 导入/导出兜底

### 决策 3：数据格式怎么设计？

```javascript
{
  id: "1748150400000_x7k2m",   // 唯一 ID 用于删除
  name: "2605250030000387 NDIS log analysis",
  category: "work",             // work | break | study | other
  start: 1748150400000,         // 毫秒时间戳
  end:   1748151230000
}
```

**关键设计**：
- `name` 是自由文本，**不强制结构**——Case ID 通过正则后处理识别，而不是要求用户手动选项目。这是降低摩擦的核心妥协。
- 用毫秒时间戳而非格式化字符串——可以做任意精度的运算
- 单条记录无依赖——可以任意删除、任意时间排序

### 决策 4：分类怎么定？

初版我做了 `work / break / other` 三类。后来用户提出加 `study`，于是变成 4 类。

**没有做的事**：
- ❌ 让用户自定义分类（增加配置复杂度，违背 0 学习成本）
- ❌ 二级分类（如 work → research / coding / email）（柳比歇夫式的"事件颗粒度"已经体现在任务名里了）

**取舍逻辑**：分类只是用来画总览饼图、看工作-休息比例。**细节归类交给任务名 + Case ID**，分类不需要承担过多职责。

---

## 五、迭代过程：需求是怎么演化的

我做了一个表，记录每次迭代的触发点和决策：

| # | 触发 | 加了什么 | PM 视角的反思 |
|---|---|---|---|
| 1 | 我自己每天记不准 labor | MVP（计时 + 今日汇总） | 自己是第一用户，dogfooding 是最快的反馈源 |
| 2 | 想看一周趋势 | 趋势图（堆叠柱图） | 柳比歇夫的"周/月复盘"环节，必须有 |
| 3 | "怎么开关 app 顺手点？" | 桌面快捷方式 + Ctrl+Alt+T 热键 | 把"打开 app"这步的摩擦从 5 秒降到 0.3 秒 |
| 4 | "想分享给同事用" | 打包 zip + 一键安装脚本 + GitHub Pages | 分发是产品的一部分，不是事后想 |
| 5 | "番茄钟会不会打断我？" | 番茄钟改为**纯提醒**，不停任务计时 | **用户的真实工作场景比理论模型重要**——跟客户打电话该算 work 就算 work |
| 6 | "我在好几个浏览器开过，提醒会不会被错过？" | 任务栏闪烁 + 全屏红色覆盖 + 循环响铃 | 强提醒系统是真痛点，不是过度设计 |
| 7 | "学习时间想单独统计" | 加 study 分类 | 分类要克制，但确实有空白时加上 |
| 8 | **"Case ID 能自动归类吗？"** | 16 位数字正则自动识别 + 按 Case ID 汇总 | **杀手锏功能** |

### 关于第 8 步的特别说明 ⭐

这个功能是用户提出来后我才意识到的。如果早期产品定义时我能想到这一点，整个产品的"价值密度"会高一个量级。

**教训**：
- PM 最大的盲点是"自己也是用户但还是想不到的需求"
- **持续暴露给真实用户是发现金矿的唯一方法**
- 一旦发现金矿要果断重新定位价值主张——现在 Time Tracker 的对外宣传里 "Case ID 自动归类" 已经从"小功能"提到了"核心亮点 #2"

---

## 六、工具链选择：为什么我能半天做完？

这部分是**给想自己动手的工程师**看的。

| 环节 | 选用的工具 | 为什么 |
|---|---|---|
| **需求 → 代码** | GitHub Copilot CLI (Claude Opus) | 自然语言对话生成完整 HTML/JS，节省 80% 编码时间 |
| **图表** | Chart.js (CDN 引入) | 0 配置、图表种类够用、轻量 |
| **数据存储** | 浏览器原生 localStorage | 0 依赖、API 极简、跨域不需考虑 |
| **桌面集成** | PowerShell + WScript.Shell 创建快捷方式 | Windows 原生能力，无需第三方工具 |
| **应用窗口** | Edge `--app=` 命令行参数 | 把网页伪装成原生 app，免开发 Electron |
| **版本管理** | Git + GitHub | 同时支持分享、协作、回滚 |
| **公开发布** | GitHub Pages | 静态网页免费托管、配置极简、HTTPS 自动 |
| **打包分发** | PowerShell `Compress-Archive` + 安装脚本 | 给不会用 Git 的同事提供傻瓜方案 |

### 关键洞察 💡：**工具组合的选择，决定了项目能不能在"半天"做完**

如果我选了 Electron + 自建后端 + Postgres + 用 webpack 打包，**同样的功能可能要做一周**。
如果我选了 Notion / Airtable 这种 no-code，**Case ID 正则识别这种自定义逻辑根本做不了**。

**最佳点位 = 用最简单工具能实现核心需求的那个点**。多一分都是过度设计，少一分就解决不了问题。

---

## 七、反思与教训

### 做对的事
- ✅ **从真实痛点出发**：自己是第一用户，需求验证最快
- ✅ **极致克制**：每加一个功能都问"这是核心痛点的解吗？"
- ✅ **MVP 不等于丑陋**：UI 设计花了 30% 的时间，因为美观本身降低使用摩擦
- ✅ **分发当成产品的一部分**：打包脚本、桌面快捷方式、README 这些都不是"事后想到的事"

### 走过的弯路
- ❌ **早期把番茄钟跟任务计时强绑定**：用户一句"我跟客户打电话不能停"立刻意识到错了，回滚为纯提醒
- ❌ **早期没想到 Case ID 自动识别**：这是产品的最大亮点之一，但靠用户反馈才发现
- ❌ **强提醒系统第一次做太弱**：只有标题闪烁，用户在视频会议里完全感知不到，后来才加上任务栏闪烁 + 全屏覆盖

### 如果再做一次
1. **第一天就把"自动归类"功能纳入 MVP**——任何能做自动化的事，都比让用户手动做好
2. **更早做用户访谈**——不是问"你想要什么功能"，而是问"上一次你抓狂的瞬间是什么"
3. **提前想好"放弃路径"**：万一用户量起来了我要不要做云同步？数据要不要加密？这些边界要早想清楚

---

## 八、产品的"性价比模型"

最后用一个表格总结这个项目的产品 ROI：

| 指标 | 数值 |
|---|---|
| 开发时间 | ~ 半天（含迭代）|
| 代码量 | ~ 800 行 JS + 200 行 HTML/CSS |
| 部署成本 | 0（GitHub Pages 免费）|
| 维护成本 | 0（纯静态网页）|
| 用户安装步骤 | 1 步（双击桌面图标 / 打开网址）|
| 用户学习曲线 | 0（输入 + 回车）|
| 解决的痛点 | 工时记录不准、case 归类难、缺乏复盘工具 |
| 可分享性 | ⭐⭐⭐⭐⭐（一个网址就能用）|

**结论**：在"个人小工具"这个细分品类，**Web + localStorage + GitHub Pages** 的组合可能是性价比最高的方案。

---

## 九、下一步

如果未来要继续演化，我会按这个顺序考虑：

1. **不做云同步**——会破坏"零运维"的定位
2. **可能做 PWA 离线安装**——让手机也能"装"它
3. **可能做 Outlook 集成**——读 calendar 自动建议任务名
4. **可能做按 Case ID 的批量导出**——直接对接 D365/SAP 的 labor 录入格式
5. **可能做习惯追踪**——柳比歇夫不止记录时间，他还分析模式

但每个"可能"都要先问自己：**这是不是核心痛点的解？** 不是就丢掉。

---

## 总结

**做产品最贵的不是写代码，而是"想清楚"。**

想清楚后，工具只是手段——HTML、JS、PowerShell、Git、GitHub Pages，每个都是被刻意选中的最低成本路径。

如果你也有一个"我每天都被它折磨"的小痛点，**别等周末、别等系统化的方案**——打开 Copilot CLI 说一句"帮我做一个 X"，半天后你可能就有一个解决自己问题的小工具了。

而当你把它分享出来，可能会发现：**全世界都跟你一样在被同一件事折磨。**

---

*文档作者：Iris He Min*
*项目：https://github.com/IrisHeMin/timetracker*
*在线体验：https://irishemin.github.io/timetracker/*
*完成于：2025 年 5 月*
